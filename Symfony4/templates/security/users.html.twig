{% extends 'base.html.twig' %}

{% block title %}Tous les utilisateurs{% endblock %}

{% block header %}
    <div class="page-breadcrumb">
        <div class="d-flex align-items-center">
            <h4 class="page-title text-truncate text-dark font-weight-medium mb-0">Tous les utilisateurs</h4>
            <div class="ml-auto">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb m-0 p-0">
                        <li class="breadcrumb-item text-muted active" aria-current="page">Utilisateur</li>
                        <li class="breadcrumb-item text-muted" aria-current="page">Tous</li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
{% endblock %}

{% form_theme form 'bootstrap_4_layout.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Liste de tous les utilisateurs</h4>
                    <hr>
                    <div class="table-responsive">
                        <table class="table table-hover m-t-20">
                            <thead class="thead-light">
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Profile</th>
                                    <th scope="col">Utilisateur</th>
                                    <th scope="col">Nom et prénoms</th>
                                    <th scope="col">Rôle</th>
                                    <th scope="col">Date insertion</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for user in users %}
                                <tr>
                                    <th scope="row">{{ loop.index }}</th>
                                    <td>
                                        <img src="{{ asset(user.photo) }}" alt="user" width="30" class="rounded-circle">
                                    </td>
                                    <td>{{ user.username }}</td>
                                    <td>{{ user.nom ~ ' ' ~ user.prenom }}</td>
                                    <td>{{ user.fonction |title }}</td>
                                    <td>
                                        {{ user.date |date('d/m/Y') }}
                                   
                                        <span class="float-right actions">
                                            <a href="{{ path('user_profile', {id: user.id}) }}" class="mr-1" data-toggle="tooltip" title="Voir profil"><i class="mdi mdi-eye"></i></a>
                                        </span>
                                        
                                    </td>
                                </tr>                            
                            {% endfor %}
                            </tbody>
                        </table>
                    </div> 
                </div>
            </div>

            <div class="card shadow mb-4">
                <!-- Card Header - Accordion -->
                <a href="#collapseCardExample" class="d-block card-header py-3" data-toggle="collapse" role="button" aria-expanded="true" aria-controls="collapseCardExample">
                    <h6 class="m-0 font-weight-bold">Ajouter nouveau utilisateur</h6>
                </a>
                <!-- Card Content - Collapse -->
                <div class="collapse" id="collapseCardExample" style="">
                    <div class="card-body">
                        <div class="row">
                            <div class="col-md-12">
                                {{ form_start(form) }}
                                <div class="row">
                                    <div class="col-md-6">
                                        {{ form_row(form.username, { label: "Login *" }) }}
                                        {{ form_row(form.nom, { label: "Nom *" }) }}
                                        {{ form_row(form.prenom, { label: "Prénom *" }) }}
                                        {{ form_row(form.email, { label: "Email" }) }}
                                        {{ form_row(form.phone, { label: "Numero téléphone *" }) }}
                                    </div>
                                    <div class="col-md-6">
                                        {{ form_row(form.password, { label: "Mot de passe *" }) }}
                                        {{ form_row(form.confirm_password, { label: "Confirmation mot de passe *" }) }}
                                        {{ form_row(form.fonction, { label: "Rôle dans la Mutuelle" }) }}
                                        {{ form_row(form.photo, { label: "Photo de profil" }) }}
                                    </div>
                                </div>                      
                                <div class="form-row">
                                    <div class="col-12 text-right">
                                        <button type="submit" class="btn btn-success"><i class="ti-save"></i> Enregistrer</button>
                                    </div>
                                </div>                                                     
                                {{ form_end(form) }}
                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
            {#
            <div class="card">
                <div class="card-body">
                    <h4 class="card-title">Nouveau utilisateur</h4>
                    <hr>
                    <div class="row">
                        <div class="col-md-12">
                            {{ form_start(form) }}
                            <div class="row">
                                <div class="col-md-6">
                                    {{ form_row(form.username, { label: "Login *" }) }}
                                    {{ form_row(form.nom, { label: "Nom *" }) }}
                                    {{ form_row(form.prenom, { label: "Prénom *" }) }}
                                    {{ form_row(form.email, { label: "Email" }) }}
                                    {{ form_row(form.phone, { label: "Numero téléphone *" }) }}
                                </div>
                                <div class="col-md-6">
                                    {{ form_row(form.password, { label: "Mot de passe *" }) }}
                                    {{ form_row(form.confirm_password, { label: "Confirmation mot de passe *" }) }}
                                    {{ form_row(form.fonction, { label: "Rôle dans la Mutuelle" }) }}
                                    {{ form_row(form.photo, { label: "Photo de profil" }) }}
                                </div>
                            </div>                      
                            <div class="form-row">
                                <div class="col-12 text-right">
                                    <button type="submit" class="btn btn-success"><i class="ti-save"></i> Enregistrer</button>
                                </div>
                            </div>                                                     
                            {{ form_end(form) }}
                        </div>
                        
                    </div>
                </div>
            </div>#}
        </div>
    </div>
{% endblock %}